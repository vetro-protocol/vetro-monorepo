// TODO: read the current epoch with `getEpochId` from
// `@vetro-protocol/target-yield-earn` once the vault is deployed. The contract
// returns a uint256, and 0 means no epoch has started yet.
export const fetchEpochId = async () => 1n;
